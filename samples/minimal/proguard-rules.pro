# Sample app R8 rules. The DeviceIntelligence library ships its own
# consumer-rules.pro (JNI surface, key assembler, init provider),
# auto-applied here. Nothing app-specific to keep — the launcher Activity is
# kept automatically from the merged manifest.
