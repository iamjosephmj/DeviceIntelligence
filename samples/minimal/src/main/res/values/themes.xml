<resources>
    <!--
      Dark theme, matching IntelColors. The window background agrees with the
      Compose background so the launcher-to-app handoff shows no white flash.
      NoActionBar because every screen draws its own pinned header.

      The bar colours are set to the page background rather than transparent:
      enableEdgeToEdge() takes over the system bars at runtime, and these only
      cover the frames before it runs.
    -->
    <style name="Theme.Sample" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@color/sample_page_bg</item>
        <item name="android:statusBarColor">@color/sample_page_bg</item>
        <item name="android:navigationBarColor">@color/sample_page_bg</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
        <!--
          Default activity transition: a quick cross-fade in / out so
          the launcher-to-app handoff doesn't feel abrupt. The fade
          duration is taken from the platform's
          shortAnimTime (~150ms).
        -->
        <item name="android:windowAnimationStyle">@style/SampleWindowAnimations</item>
    </style>

    <style name="SampleWindowAnimations" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/fade_in</item>
        <item name="android:activityOpenExitAnimation">@anim/fade_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/fade_in</item>
        <item name="android:activityCloseExitAnimation">@anim/fade_out</item>
    </style>
</resources>
