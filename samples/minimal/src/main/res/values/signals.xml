<?xml version="1.0" encoding="utf-8"?>
<!-- GENERATED by tools/registry/gen-signal-catalogue.py — do not edit. -->
<!-- TESTBED ONLY (worst-case narrative 2026-09-10, R8): this catalogue bakes the
     FULL detector prose into the app's resources and dex. Fine for this lab app;
     a PRODUCTION app must NOT copy it — that ships the evasion cookbook in
     plaintext to every attacker with apktool. Production renders prose server-side. -->
<resources>
    <string name="signal_group_attestation">Attestation</string>
    <string name="signal_group_attestation_blurb">Hardware identity and verified boot: whether the device is what it claims to be.</string>
    <string name="signal_group_runtimeinstrumentation">Runtime instrumentation</string>
    <string name="signal_group_runtimeinstrumentation_blurb">Hooks, debuggers and injected code: whether this process is being manipulated as it runs.</string>
    <string name="signal_group_antianalysis">Anti-analysis</string>
    <string name="signal_group_antianalysis_blurb">Syscall filtering: whether something is hiding what the process can observe.</string>
    <string name="signal_group_rootandsystem">Root and system integrity</string>
    <string name="signal_group_rootandsystem_blurb">Whether the operating system underneath the app is still trustworthy.</string>
    <string name="signal_group_packageintegrity">Package integrity</string>
    <string name="signal_group_packageintegrity_blurb">Whether the running APK and its DEX are the ones that were signed.</string>
    <string name="signal_group_virtualenvironments">Virtual environments</string>
    <string name="signal_group_virtualenvironments_blurb">Whether this device is real silicon or an emulated/translated environment.</string>

    <string name="intel_0000_meaning">TEE key-attestation cross-checks failed (keybox reuse / device-property honeypot / revoked keybox); the device\'s hardware-backed identity is untrustworthy.</string>
    <string name="intel_0000_reach">Forged hardware identity — spoof Play Integrity DEVICE/STRONG, impersonate a genuine device, defeat hardware-key-bound checks.</string>
    <string name="intel_0001_meaning">An ArtMethod entry point, JNIEnv table, inline prologue, or ACC_NATIVE bit was tampered (Xposed/LSPosed/frida-family hook engine).</string>
    <string name="intel_0001_reach">Arbitrary Java/Kotlin method interception → in-app code injection — steal credentials, rewrite business logic, bypass in-app checks.</string>
    <string name="intel_0002_meaning">Live in-memory .text of the native core diverged from the build-baked hash (patched or replaced native library).</string>
    <string name="intel_0002_reach">Native code injection — the detector\'s own logic can be patched out.</string>
    <string name="intel_0003_meaning">A monitored native function\'s prologue was overwritten with a trampoline (inline hook).</string>
    <string name="intel_0003_reach">Native function interception — redirect calls/syscalls into injected code.</string>
    <string name="intel_0004_meaning">A seccomp filter configured to kill the process on syscalls was detected (anti-instrumentation / sandbox tamper).</string>
    <string name="intel_0004_reach">Anti-instrumentation — the process self-kills to evade analysis / sandboxing.</string>
    <string name="intel_0005_meaning">A tracer/debugger is attached to the process (non-zero TracerPid / ptrace).</string>
    <string name="intel_0005_reach">Live memory &amp; key extraction, step-through dynamic tampering.</string>
    <string name="intel_0006_meaning">A frida-server listening port was found.</string>
    <string name="intel_0006_reach">Dynamic instrumentation — hook anything in the process, full code injection.</string>
    <string name="intel_0007_meaning">A frida worker thread (pool-frida) was found in the process.</string>
    <string name="intel_0007_reach">In-process Frida instrumentation → code injection.</string>
    <string name="intel_0008_meaning">A known hook-framework library (dobby/whale/yahfa/substrate/...) was mapped into the process.</string>
    <string name="intel_0008_reach">Arbitrary function hooking → code injection.</string>
    <string name="intel_0009_meaning">A simultaneously writable+executable mapping was found — proof-positive on API &gt;= 29 where W^X is enforced. Read-write-executable memory mapping detected. On API 28+ the loader and the ART JIT do not leave a persistent RWX page, so one is a strong hooker signature — but RWX alone is ambiguous (a JIT code cache is RWX too), so each region is characterized: a region whose trampoline stubs branch into legit system code or a foreign module (tramp=legit/foreign, hook_stub_regions&gt;0) is a hook redirecting real code; a self-referential or empty region (tramp=self/none) is a benign cache.</string>
    <string name="intel_0009_reach">Injected shellcode / self-modifying hook staging.</string>
    <string name="intel_0010_meaning">A frida memfd-backed JIT mapping was found. Frida 16+ Gum JIT signature: rwxp-mapped /memfd:jit-cache region(s) &gt; 8 MB. ART legitimately maps the same path but only with r-xp/r--p perms; the rwxp combination is unambiguous on Android.</string>
    <string name="intel_0010_reach">Instrumentation code injection, staged from a hidden memfd.</string>
    <string name="intel_0011_meaning">An su binary was found on PATH or a common location.</string>
    <string name="intel_0011_reach">Root escalation available — unlocks essentially every tamper in this table.</string>
    <string name="intel_0012_meaning">An su binary was found in a protected system path (strong root indicator).</string>
    <string name="intel_0012_reach">Strong root → full device control.</string>
    <string name="intel_0013_meaning">A Magisk file or artifact was found on the device.</string>
    <string name="intel_0013_reach">Systemless root → module injection, Play-Integrity spoofing via modules.</string>
    <string name="intel_0014_meaning">Magisk mount traces were found in init\'s mountinfo.</string>
    <string name="intel_0014_reach">Systemless root active → hidden module mounts overlaying system files.</string>
    <string name="intel_0015_meaning">The Magisk daemon (magiskd) abstract socket was found.</string>
    <string name="intel_0015_reach">Live Magisk daemon → on-demand root grants to any app.</string>
    <string name="intel_0016_meaning">KernelSU (kernel-level root) was detected.</string>
    <string name="intel_0016_reach">Kernel-level root — the strongest tamper; can defeat userspace detection and spoof almost anything.</string>
    <string name="intel_0017_meaning">A tmpfs bind-mount was found over the conscrypt trust-store apex (TLS interception / MITM setup).</string>
    <string name="intel_0017_reach">MITM / TLS interception — network credential and traffic theft.</string>
    <string name="intel_0018_meaning">SELinux is not enforcing (enforce=0).</string>
    <string name="intel_0018_reach">Sandbox boundary removed — cross-app data access, privilege escalation.</string>
    <string name="intel_0019_meaning">The build is signed with Android test-keys (non-production / engineering build).</string>
    <string name="intel_0019_reach">Non-production / pre-rooted image — weakened security posture.</string>
    <string name="intel_0020_meaning">The running APK\'s signing certificate does not match the expected signer (repackaged / re-signed).</string>
    <string name="intel_0020_reach">Repackaged / trojanized app — injected malware, credential harvesting inside a clone.</string>
    <string name="intel_0021_meaning">A protected APK entry\'s bytes differ from the baked fingerprint (tampered package).</string>
    <string name="intel_0021_reach">Patched app logic / tampered resources or DEX.</string>
    <string name="intel_0022_meaning">An unexpected entry was added to the APK.</string>
    <string name="intel_0022_reach">Injected payload/DEX smuggled into the package.</string>
    <string name="intel_0023_meaning">An expected APK entry is missing.</string>
    <string name="intel_0023_reach">Stripped security asset / disabled check.</string>
    <string name="intel_0024_meaning">The baked APK fingerprint asset could not be parsed or validated.</string>
    <string name="intel_0024_reach">Integrity baseline damaged — self-check degraded.</string>
    <string name="intel_0025_meaning">The baked APK fingerprint asset has an invalid magic header (tampered asset).</string>
    <string name="intel_0025_reach">Tampered integrity-baseline asset.</string>
    <string name="intel_0026_meaning">The APK source directory is not the expected install location.</string>
    <string name="intel_0026_reach">Side-loaded / hijacked install path.</string>
    <string name="intel_0027_meaning">The installing package is not an approved app store.</string>
    <string name="intel_0027_reach">Side-loaded outside a trusted store.</string>
    <string name="intel_0028_meaning">A DEX was loaded from an attacker-writable path (runtime code injection).</string>
    <string name="intel_0028_reach">Runtime Java/Kotlin code injection — load attacker classes into the app.</string>
    <string name="intel_0029_meaning">The process holds a SECCOMP_RET_USER_NOTIF listener fd — an in-process syscall interceptor used to spoof /proc reads below libc (maps/status/mounts). A benign seccomp-bpf policy leaves no such fd.</string>
    <string name="intel_0029_reach">In-process syscall interceptor — spoof /proc reads (maps/status/mounts) *below libc*, hiding root from even raw-syscall probes.</string>
    <string name="intel_0030_meaning">The device self-reports a clean, locked boot (ro.boot.verifiedbootstate=green with flash.locked=1 or vbmeta.device_state=locked) while its hardware attestation RootOfTrust says otherwise — a prop-spoofing Play-Integrity-Fix (e.g. IntegrityBox/TrickyStore) forcing the boot-state props while the TEE reports the truth.</string>
    <string name="intel_0030_reach">Verified-boot / Play Integrity spoofing — a rooted device masquerades as a locked, uncompromised one.</string>
    <string name="intel_0031_meaning">RETIRED: the leaf-notBefore-precedes-issuer heuristic false-positives on genuine StrongBox/TEE devices, whose attestation leaf legitimately carries notBefore=epoch-0 (1970) while the per-attestation issuer intermediate is freshly minted — so leaf.notBefore &lt; issuer.notBefore is the normal genuine case, not a spoof tell. Code reserved so wire ids never shift. Keybox injection is covered by INTEL_0030 (boot-state self-report vs hardware RootOfTrust) plus CRL revocation and cross-level batch-key-reuse checks.</string>
    <string name="intel_0032_meaning">The StrongBox and TEE attestation chains were signed by the SAME batch key. Genuine hardware provisions physically-separate, distinct batch keys per security level; a single leaked keybox can only sign with one key, so identical batch keys across levels prove keybox injection. Live-confirmed: spoofed rig reused one TEE keybox for both levels (identical batch pubkey) while a genuine StrongBox device presented distinct O=StrongBox / O=TEE batch keys.</string>
    <string name="intel_0032_reach">Injected leaked keybox — spoof hardware attestation end-to-end, defeat Play Integrity STRONG.</string>
    <string name="intel_0033_meaning">No StrongBox attestation chain was produced even though StrongBox appears to be available, so the cross-level keybox-reuse defense (INTEL_0032) could not run (fail-closed). Two routes reach this: (a) the leaf claims StrongBox assurance but no StrongBox cross-level chain was produced; (b) the device reports StrongBox hardware (FEATURE_STRONGBOX_KEYSTORE) yet enrollment fell back to TEE. Route (b) applies ONLY to models absent from the StrongBox capability list — a capable model presenting TEE-only is the CRITICAL INTEL_0034 downgrade instead, regardless of what the device reports, because that self-report is app-visible and hookable. Lower confidence than INTEL_0032: a transient StrongBox attestation failure on a GENUINE device causes both routes, so this is a candidate for observe-only / retry rather than a hard block.</string>
    <string name="intel_0033_reach">Cross-level keybox-reuse check (INTEL_0032) could not run — possible attestation-downgrade evasion (also fires transiently on genuine HW).</string>
    <string name="intel_0034_meaning">RETIRED. The StrongBox-capability interlock: a TEE-only presentation whose ATTESTED brand/model was a device known to ship StrongBox. Retired with its backing data, verifier/src/main/resources/strongbox-devices.json — a hand-curated device list that FAILED OPEN on every unlisted model (it held four rules: Pixel 3-19, SM-S9xx, SM-F9xx, one Xiaomi), carried an unbounded maintenance cost, and whose own documented production path (\'back this with a maintained device-capability source\') had no API: StrongBoxCapability was a singleton with a hardcoded resource path and no injection point, unlike the injectable pinnedRoots and CRL. COVERAGE LOST, recorded deliberately: INTEL_0032 (keybox_cross_level_reuse) CANNOT fire on a TEE-only presentation — crossLevelCheck returns early when the StrongBox chain is absent — so nothing now structurally catches a pure attestation downgrade carried by a fresh, unrevoked leaked keybox with consistent properties. A per-device assurance high-water mark (issue #9) would restore the capability without any device list. Code reserved as a tombstone; never reuse this id.</string>
    <string name="intel_0035_meaning">Executable code is mapped from outside the legitimate code roots (system/apex/vendor/product/app) — an injected native library such as a Zygisk/Magisk/KernelSU module. Provenance-based and name-independent: unlike the fixed hook-framework name-list (INTEL_0008), this catches ANY injected .so by where it came from. Live-verified catching hma/lsposed-vector/playintegrityfix/zygisksu + a custom module by /data/adb path alone.</string>
    <string name="intel_0035_reach">Native code injection (Zygisk/Magisk/KernelSU module) by provenance, name-independent.</string>
    <string name="intel_0036_meaning">A pointer inside a system/app library\'s data or GOT points into injected foreign executable code — a hooked function pointer. False-positive-free: no legitimate library ever references an injected module. Complements INTEL_0035; catches a hook whose module hides its .so but still redirects a GOT slot into foreign code.</string>
    <string name="intel_0036_reach">Hooked function pointer — calls silently redirected into injected code.</string>
    <string name="intel_0037_meaning">A foreign (injected) native library declares a DT_NEEDED dependency on a DEDICATED inline-hooking library (dobby/whale/yahfa/lsplant/substrate/shadowhook/bytehook/xhook). These libraries exist only to rewrite other code, so a foreign module linking one is a confirmed hooking framework. Parsed from the already-mapped ELF in memory (no root file access); zero generic-import heuristics, so no false positives on ordinary vendor libraries. RETIRED: reworked into fail-open ENRICHMENT on INTEL_0035 (module_id + soname + DT_NEEDED read from the mapped image) rather than a separate verdict-driving detection — the raw DT_STRTAB deref was a crash/DoS vector on malformed ELF and strstr matching risked substring false positives; enrichment describes the already-flagged module instead of condemning it.</string>
    <string name="intel_0038_meaning">A hot libc function\'s prologue is overwritten with a branch into injected foreign code (directly into a foreign .so, or via an anon trampoline that references a foreign module) — an inline hook (Dobby/ShadowHook/bytehook/whale). Unlike INTEL_0036 (GOT/function-pointer hijack), an inline hook leaves the GOT intact and patches the function body, so the pointer scan cannot see it; this decodes the first instruction of a fixed hot-set (openat/access/__system_property_get/ioctl/ptrace/fork/…) and flags one only when it is an unconditional branch (direct B, or LDR x16/x17 + BR absolute trampoline). False-positive-safe via the INTEL_0036 discipline: the branch target must resolve to injected code OUTSIDE the legit roots (a direct foreign .so, or an anon trampoline that references one), so a legitimately app-bundled inline hooker — whose trampoline routes to its own /data/app .so — is NOT flagged; a normal libc prologue (never an unconditional branch) and an intra-legit ifunc/tail branch are also ignored. Enriches with the hooked symbol and, via the anon-trampoline hop, the hooking module.</string>
    <string name="intel_0038_reach">libc interception — hide root and lie to the app about files/properties/syscalls.</string>
    <string name="intel_0039_meaning">A hot libc function\'s prologue is an absolute-jump trampoline stub (arm64 LDR x16/x17 + BR; x86_64 jmp rel32 / jmp [rip] / movabs+jmp / push+ret; arm LDR pc) that inline-hooking engines (Dobby/ShadowHook/bytehook/Frida Gum/Substrate) splice over a function entry. Detected by OPCODE SHAPE alone — no baseline, no baked hash, no on-disk comparison — so it is immune to per-device/per-version libc drift and fires even on a function hooked before JNI_OnLoad. Complements INTEL_0038 (which anchors on provenance: the branch must target injected foreign/anon code): INTEL_0039 catches the SAME inline hook when every file-backed trace has been scrubbed and the trampoline references only legit code (Shamiko-grade cleanup), because the stub must remain in place for the hook to function. False-positive-safe by construction: a legitimately-compiled prologue is stack setup or a BTI landing pad, never an absolute-jump stub, and benign code drift never produces one. Promoted to CRITICAL after clean-device FP validation on three OEMs (Pixel 6 Pro, Pixel 9 Pro, Xiaomi 2512BPNDAG); a legit app-bundled inline hooker also splices a stub, which is a policy call rather than a benign-drift false positive.</string>
    <string name="intel_0039_reach">Inline libc hook that survives provenance cleanup (Shamiko/NeoZygisk-grade) — filesystem/syscall lies.</string>
    <string name="intel_0040_meaning">a libc file-query (faccessat or the stat family) reports an existing system file (e.g. /system/bin/sh, /apex/com.android.runtime) as absent while a raw `svc` faccessat syscall — which bypasses libc entirely — confirms it exists. The only cause is a userspace hook (inline / GOT / PLT / LD_PRELOAD) lying about the filesystem to hide root or tamper artifacts. Unlike INTEL_0036/0038/0039 which detect a hook STRUCTURALLY (GOT pointer / inline prologue stub), this is BEHAVIORAL and mechanism-independent: it catches any hooking method as long as the observable result diverges from kernel truth, including hooks those structural signals miss (a GOT hook with an intact prologue, a handler in anon-legit memory, a preload interposer). False-positive-free by construction: libc and the raw syscall issue the same call with the same args, so they agree on a clean device; only a kernel-confirmed existence (raw==0) is trusted as ground truth, and a failed/unavailable raw call is never flagged. Carries hooked_symbol=faccessat so the verdict can CORRELATE it with a structural hook on the same symbol (structural + behavioral agreement = definitive). Boundary: catches userspace hooks only; a kernel/seccomp-level filter intercepts the raw svc too and is covered by the proc-consistency signal instead.</string>
    <string name="intel_0040_reach">Filesystem cloaking — hide root binaries / tamper artifacts from the app (behavioral, mechanism-independent).</string>
    <string name="intel_0041_meaning">The dynamic linker (dl_iterate_phdr) names a loaded .so whose executable segment maps, in /proc/self/maps, to an ANONYMOUS empty-path VMA. This is the file-&gt;anon map spoof performed by NeoZygisk\'s spoof_virtual_maps() (and ZygiskNext-class cleanup): after modules unload it mremaps the injected loader\'s file-backed VMA into an anonymous region to erase the foreign-code provenance that INTEL_0035 keys on — but the linker\'s soinfo still names the object, so the two views split. This is the residue that survives the cleanup specifically designed to blind file-provenance detection. False-positive-free by construction: a legitimately loaded .so always has BOTH a linker record AND a file-backed VMA at its base; a memfd-loaded library keeps its /memfd: name (not empty), and JIT / trampoline anonymous code is never linker-tracked so it does not appear in dl_iterate_phdr. Complements INTEL_0035 (pre-cleanup file provenance) and INTEL_0040 (behavioral backstop). Validated: 0 divergences across 349 legitimate linker objects on a clean device; fires on a spoof_virtual_maps-style anonymization.</string>
    <string name="intel_0041_reach">Stealth injected module with provenance erased (NeoZygisk spoof_virtual_maps-class hiding).</string>
    <string name="intel_0042_meaning">A sealed (F_SEAL_WRITE|F_SEAL_SEAL) EXECUTABLE memfd is mapped into the process. Root injectors (NeoZygisk / zygiskd) hand each Zygisk module .so to the app as a sealed read-only memfd received over a unix socket and dlopen it (DlopenMem) — so the module never appears at a /data/adb path that INTEL_0035\'s file provenance keys on. Detection reads /proc/self/fd for memfd symlinks, fcntl(F_GET_SEALS), and cross-checks /proc/self/maps for a matching executable /memfd: region. Name-independent: it keys on the SEAL BITS, so a module whose memfd is named \'jit-cache\' to masquerade as ART\'s JIT cache is still caught — because ART\'s JIT memfd is executable but WRITABLE (never F_SEAL_WRITE). False-positive-safe: ordinary apps do not map sealed executable memfds. Complements INTEL_0041 (the anonymized loader) — together they cover both provenance-evasion mechanisms (anon loader + memfd modules) that a /data/adb-centric provenance scan misses. Validated: 0 on a clean app; caught a sealed exec memfd named \'jit-cache\'.</string>
    <string name="intel_0042_reach">memfd-loaded injected module with no on-disk path — stealth native injection (keys on seal bits, so a jit-cache-named decoy is still caught).</string>
    <string name="intel_0043_meaning">A boot-state / root-indicator property (ro.boot.verifiedbootstate, ro.boot.flash.locked, ro.boot.vbmeta.device_state, ro.build.tags, ro.debuggable, ...) read via libc __system_property_get returns a DIFFERENT value than the same property read from the property area via __system_property_find + __system_property_read_callback. The only cause is a userspace hook on __system_property_get lying about the property — in-process boot-state / verified-boot spoofing (the behavioral, provenance-independent analog of INTEL_0030\'s attestation-vs-self-report check). Mechanism-independent: it survives injectors that hide their module\'s provenance (NeoZygisk-class) because it tests the RESULT of the property read, not the hook\'s origin. False-positive-free by construction: both entry points read the same property trie, so they agree on a clean device (boot-state properties are static); only a selective hook on the legacy __system_property_get entry point diverges. (A resetprop-style edit of the trie itself changes BOTH reads and is caught by the attestation layer, not here.) Complements INTEL_0040 (syscall divergence).</string>
    <string name="intel_0043_reach">In-process boot-state / verified-boot spoofing at the property layer (the behavioral analog of INTEL_0030).</string>
    <string name="intel_0044_meaning">The attestation\'s KeyDescription explicitly reports securityLevel=Software(0): the key was generated by the AOSP software keystore, not a TEE or StrongBox, so this environment has NO hardware root of trust. Emulators (AVD, Genymotion, BlueStacks, cloud farms) and devices without a hardware keystore land here. Unlike environment probes — build properties, sensors, telephony, graphics libraries — this cannot be neutralised by an anti-emulation layer: evading it requires possessing a Google-signed hardware attestation key, i.e. actually being real hardware. Fires ONLY on an explicitly parsed securityLevel of 0; a missing or unparseable level grades to Assurance.SOFTWARE for the integrity gate but does NOT raise this signal, so the label never states more than the attestation proved. Note the precise claim is \'no hardware root of trust\', not \'is an emulator\' — a genuine device with a broken or non-Google keystore also lands here.</string>
    <string name="intel_0044_reach">Emulator / software keystore — no TEE at all.</string>
    <string name="intel_0045_meaning">The scan\'s self-reported package name or signing-certificate digest does not match the attestationApplicationId (tag 709) recorded from the hardware attestation at bootstrap. The self-report is app-visible and trivially patchable; the attested value is written by the platform keystore and signed over by the TEE. A disagreement means the running code is not the code the attested key was issued to - repackaging, a cloned or rehosted APK, or a patched core reporting a licensed identity it does not have. The self-report alone proves nothing: this signal is the CROSS-CHECK, and only the disagreement is evidence. Trustworthy exactly when the boot state is, since tag 709 lives in the softwareEnforced list.</string>
    <string name="intel_0045_reach">Repackaged or rehosted APK — the running code is not what the attested key was issued to.</string>
    <string name="intel_0046_meaning">The self-reported and attested app identities agree, but the signing-certificate digest is not in the licensed set for this deployment. This is a LICENSING finding, not a tampering one: the app is what it claims to be, it is simply not entitled to enroll. Kept separate from INTEL_0045 so a stale licence table cannot be mistaken for an integrity compromise.</string>
    <string name="intel_0046_reach">Licensing, not tampering — kept separate so a stale licence table can\'t read as a compromise.</string>
    <string name="intel_0047_meaning">The oldest of the TEE-attested osPatchLevel, vendorPatchLevel and bootPatchLevel is older than the backend\'s policy window (default 365 days), modelled on the recent-security-update requirement of Play Integrity\'s MEETS_STRONG_INTEGRITY. The threshold lives on the BACKEND, not the device: a device-side constant would be frozen at build time, would itself go stale, and could be patched out. Evaluated against the OLDEST of the three because a current framework patch on a two-year-old bootloader is still exposed. Note leaked keyboxes tend to originate from older handsets and the source handset\'s patch level rides along in the replayed certificate, so this partially correlates with INTEL_0032.</string>
    <string name="intel_0047_reach">Unpatched device — known CVEs. Backend-computed, so the window retunes without an app release.</string>
    <string name="intel_0048_meaning">Build.VERSION.SECURITY_PATCH, truncated to YYYY-MM, does not match the TEE-attested osPatchLevel. Both are the FRAMEWORK patch, which is the only apples-to-apples pair: vendorPatchLevel and bootPatchLevel are the SoC and bootloader patches and legitimately diverge on non-Pixel OEMs. A prop spoofer that fakes a current patch level to look healthy trips this, in the same family as INTEL_0030. Fails open: a missing or unparseable value on either side emits nothing.</string>
    <string name="intel_0048_reach">Prop spoofer faking a current patch to look healthy — same family as INTEL_0030.</string>
    <string name="intel_0049_meaning">A dex was loaded from memory by a class loader whose parent chain does not include the app\'s own loader, so the app\'s classes are invisible to it. A legitimate dynamic-feature or DI loader is parented into the app loader precisely so its code can call back into the app; code that cannot see the app it was loaded into is not a feature module.</string>
    <string name="intel_0049_reach">Injected code running in-process — an agent loaded its own classes without going through the app\'s loader, and can run arbitrary Java and reach whatever reflection reaches, while staying outside the app\'s class graph.</string>
    <string name="intel_0050_meaning">/proc/self/maps shows more in-memory dex mappings than any reachable class loader claims. A detached loader — held by an injector and referenced by nothing in the app — leaves exactly this trace. Compared one-directionally: ART dedupes identical dex bytes, so the mapped count can under-report, which fails safe.</string>
    <string name="intel_0050_reach">Hidden injected classes — code is loaded into the process that no reachable loader admits to, so enumerating class loaders or allow-listing them cannot see it.</string>
    <string name="intel_0051_meaning">libart.so\'s executable segment differs from the pristine on-disk file, i.e. code inside the ART runtime was rewritten after load. This is where Frida-Java\'s `implementation=` hook lands when the target method is already native: it patches ART\'s dispatch trampolines (art_quick_resolution_trampoline / art_quick_generic_jni_trampoline / art_quick_to_interpreter_bridge) and leaves the ArtMethod untouched, so the ART method registry cannot see it. Symbol-free and version-independent (bytes vs file; the art_quick_* stubs are hidden and absent from .dynsym, so no symbol-based check can reach them). The on-disk read uses raw syscalls, so a libc hook cannot serve a clean copy. CRITICAL requires an absolute-jump stub branching outside libart; any other drift is reported HIGH.</string>
    <string name="intel_0051_reach">Method interception no ART-level check can see — the hook lives in the runtime\'s dispatch path rather than the method registry, so INTEL_0001 cannot reach it. The stubs are hidden and absent from .dynsym, so no symbol-based check can either.</string>
    <string name="intel_0052_meaning">setSession() could not produce a hardware-attested key — the TEE/StrongBox keygen failed or was hooked — so the scan carries no attestation chain and cannot be bound to the session. The token is emitted anyway, degraded, rather than suppressed.</string>
    <string name="intel_0052_reach">Unbound token — nothing ties this scan to a session or to hardware, so it is replayable against another session. A hook engine that breaks keygen produces exactly this, which is why it is reported rather than met with silence.</string>
    <string name="intel_0053_meaning">scan() ran with no session id prepared: setSession() was never called, failed, or named a different session. Nothing binds this scan to a session, so the token is degraded rather than suppressed.</string>
    <string name="intel_0053_reach">Unbound token — no session binding at all, so the result cannot be attributed to a login and is replayable. Also fires on an integration that simply skipped setSession, so rule out a wiring fault before reading it as an attack.</string>
    <string name="intel_0054_meaning">The licence blob parsed but was rejected at scan time (expired, or bound to a different package). Has a benign cause — a genuinely expired blob — as well as a hostile one, so it grades below the other degradation codes.</string>
    <string name="intel_0054_reach">Licensing, or a rehosted SDK — an expired blob is benign; one bound to another package means this SDK is running in an app it was not issued to. Grades below the other degradation codes because the benign cause is common.</string>
    <string name="intel_0055_meaning">No signing key was available at all — neither the attested session key nor a plain Keystore key — so the token carries an empty SIG binding. Its contents are unauthenticated and must never read as evidence of a clean device.</string>
    <string name="intel_0055_reach">Unauthenticated token — anyone able to encrypt to the server key could have minted it naming any session. Its contents must never read as evidence of a clean device.</string>
    <string name="intel_0056_meaning">Definitional emulator signal, not a heuristic: the kernel\'s ISA (raw uname(2) syscall) does not match this process\'s compile-time ABI — an arm64 process on an x86 kernel (or the reverse) is executing under a binary-translation layer (libhoudini / libndk_translation), which is what LDPlayer / BlueStacks / Nox / MuMu ARM mode and Genymotion-on-x86 are; genuine silicon cannot produce this. Corroborating sub-facts: ro.dalvik.vm.native.bridge names a known translation bridge (no genuine phone ships one), or that bridge lib is mapped in /proc/self/maps (read via raw syscall, surviving a prop spoofer). All reads are raw syscalls — public emulator-bypass kits hook Build.* fields, __system_property_get and libc fopen/access, none the syscall layer. CRITICAL because the sub-facts are impossible on genuine silicon: the one sanctioned landing zone (ChromeOS/ARC ndk_translation) already trips INTEL_0044 software attestation and the pinned-root chain check, so it is no new blocking class, and CRITICAL is what keeps the on-device clean-device gate locked on an emulator — on an AVD where the attestation path degrades before the KeyDescription parse, this can be the only on-device finding. The device reports; the backend still decides. Fail-open on every input.</string>
    <string name="intel_0056_reach">Scaled virtual devices — one host running hundreds of \"devices\": scripted automation, replay, credential stuffing and promo abuse at near-zero marginal cost. No hardware keys (corroborate with INTEL_0044), but full app-level control.</string>
    <string name="intel_0057_meaning">Executable mapping with no backing file (or memfd/deleted backing): covers zygisk-injected stub pools (anonymous RWX), Frida gadgets (memfd), and unloaded-but-mapped payloads ((deleted)). Kernel vdso/vvar/vectors (and vsyscall/uprobes) pages are excluded by name. Reads via raw syscall, so no libc hook can serve a doctored maps blob. The device reports, the backend decides. Fail-open on unreadable maps.</string>
    <string name="intel_0057_reach">Native code injection without file provenance — code executing from mappings no clean loader produces, so path-based provenance checks (INTEL_0035) and the linker list (INTEL_0041) cannot see it.</string>
    <string name="intel_0058_meaning">Every scan carries a session-key-MACed monotonic counter — a SHA-256 chain folded over (session_key || prev_chain || be64(counter) || scan_label) — so backend replay verification detects gaps, reuse, label mismatch, or MAC forgery. The on-device rate guard caps synthetic sweep rates (the Incognia-style 60-command drive fails this). Fail-open.</string>
    <string name="intel_0058_reach">Scripted channel drive / capture — the SDK is being swept by automation rather than user-driven flows, the signature of a captured or replayed integration; the MACed counter is what backend replay verification uses to detect gaps and reuse.</string>
    <string name="intel_0059_meaning">SHA-256 over the library\'s executable segment compared to the build-time digest baked at link time — catches inline hooks and stub overwrites in the SDK\'s own code. Per-page detail is reserved for the obfuscator pass. Fail-open: a zeroed build digest (no baseline yet) or an unreadable segment contributes nothing.</string>
    <string name="intel_0059_reach">The detector itself patched while running — someone rewrote the code that reports, so every other signal from this process is suspect. Complements INTEL_0002 (build-baked APK fingerprint) with a self-contained baseline that needs no asset.</string>
    <string name="intel_0060_meaning">A fork-exec\'d watchdog child (plain /system/bin/sh, only async-signal-safe calls between fork and exec) re-reads the parent\'s /proc/&lt;pid&gt;/status TracerPid every 2 s and reports a WD line over a private pipe — an independent process attesting liveness and trace state that an in-process hook cannot forge without touching the child. One kind, two causes on the detail: cause=silent when 3 consecutive beats are missed (child killed, silenced, or never started beating — the plan\'s kill-the-child leg) and cause=tracer when the child reports a nonzero TracerPid (something is ptrace-attached to the parent right now). Each record carries a keyed-heartbeat evidence pair (seq, mac): mac = SHA-256(session_key || be32(seq)) truncated to 8 bytes, constant-time verified, consume-once per seq — the same anti-forgery/anti-replay shape as INTEL_0058\'s chain, rooted at a process-local random key until the token-crypto task rebinds it. Detection-only: no kills, no respawn. Fail-open on spawn failure and every parse error (an unreadable TracerPid field degrades to a bare liveness beat, never a finding).</string>
    <string name="intel_0060_reach">Anti-analysis against the detector itself — someone is killing or ptrace-attaching an independent witness process; treat other signals from this scan as suspect.</string>
    <string name="intel_0061_meaning">Behavioural companion to INTEL_0056: catches a translation layer that renamed itself out of the provenance keys. Two sub-facts, each architecturally impossible on genuine silicon. (1) Counter coherence: CNTVCT_EL0 must advance at exactly CNTFRQ_EL0 (a fixed always-on clock domain that does not track DVFS; measured error 0.0000 on real devices) — a bridge has no guest timer and serves the counter from the host clock (measured 23,000x divergence on libndk_translation). (2) Synchronous-fault fidelity: an undefined instruction executed by the process always reports SIGILL si_code=ILL_ILLOPC on a genuine AArch64 kernel; a bridge replays the fault from its own metadata as si_code=SI_USER, the code of a user-sent signal. Fail-open: every unreadable register, short window, or missing measurement contributes nothing. Supporting spike probes (syscall toll, cold-vs-warm) deliberately do not gate — see docs/spikes/cpu-rerouting.md.</string>
    <string name="intel_0061_reach">Hidden translation layer — a CPU-virtualizing bridge that renamed itself and scrubbed its provenance, defeating INTEL_0056\'s name/provenance keys; same reach: scaled virtual devices with full app-level control.</string>
    <string name="intel_0062_meaning">x86_64 builds only: CPUID.1:ECX[31] (hypervisor present) is set and/or the CPUID.0x40000000 hypervisor vendor leaf answers. Detects emulators that hand guest code to the real CPU under hardware virtualization (QEMU/KVM) — no binary translation exists there, so INTEL_0056 correctly stays silent and this is the only in-process tell. A real phone CPU cannot set the architecturally reserved hypervisor leaf. Honest scope: x86_64 Android also runs on Chromebooks (ARCVM) and Windows Subsystem for Android, which set the same bit — genuine markets, so the backend may downweight per policy. A determined emulator can mask the leaf (KVM cpuid masking); default configs do not. Fail-open: without the bit or a known vendor string the probe contributes nothing. arm64 builds contribute nothing by construction.</string>
    <string name="intel_0062_reach">Hardware-virtualized environment — the CPU itself is the emulator; scaled farms run these for free, and properties are the only spoofable layer (KVM can mask the leaf; default configs do not).</string>
    <string name="intel_0063_meaning">arm64 builds only: the device-tree model or compatible blob carries hypervisor-platform markers (qemu, dummy-virt, crosvm, cuttlefish, goldfish/ranchu), and/or /dev/qemu_pipe opens read-write. Catches full-system emulators and ARM VMs that run ARM app code natively inside an emulated ARM system — the guest ISA matches the app, so INTEL_0056 sees no divergence and INTEL_0062 (x86 CPUID) does not apply. Real devices report their SoC board and have no qemu_pipe node. Fails open on unreadable files; markers classified by a pure, host-tested header. Evasion note: a hypervisor that fabricates a plausible device-tree defeats this — fabrication cost is the deterrent, and the signals feed backend cross-checks against the claimed device model.</string>
    <string name="intel_0063_reach">ARM VM / full-system emulator — the arm64 counterpart of INTEL_0062 for the class TCG/ranchu farms and cuttlefish-style VMs; fails open on unreadable files, and a fabricated device-tree defeats it (fabrication cost is the deterrent).</string>
</resources>
