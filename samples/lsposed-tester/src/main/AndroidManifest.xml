<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="DI LSPosed Tester"
        android:allowBackup="false">

        <!--
        Xposed module discovery contract. LSPosed Manager scans
        every installed package for these meta-data keys; modules
        that declare them appear in the "Modules" list and can be
        enabled / scoped from there.
        -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="DeviceIntelligence anti-hooking verifier — installs LSPosed hooks on DeviceIntelligence.collect() so the StackGuard / StackWatchdog detectors emit stack_foreign_frame findings on a hooked device." />
        <meta-data
            android:name="xposedminversion"
            android:value="82" />
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />
    </application>
</manifest>
